{
  "lab_metadata": {
    "title": "Overfitting Lab — Guided Skeleton",
    "course": "Machine Learning Specialization — Course 1, Week 3",
    "type": "fill_in_the_blank",
    "language": "Python",
    "estimated_time_minutes": 60,
    "instructions": "Complete each TODO section. Run cells sequentially. Compare your outputs against the expected outputs described in each cell."
  },
  "cells": [
    {
      "cell_index": 0,
      "type": "markdown",
      "content": "# Overfitting Lab (Skeleton)\n\nComplete the marked TODO sections to build a full pipeline that diagnoses and mitigates overfitting."
    },
    {
      "cell_index": 1,
      "type": "code",
      "source": "# TODO: Import required libraries\n# Hint: numpy, matplotlib, sklearn (PolynomialFeatures, LinearRegression, Ridge, train_test_split, learning_curve, mean_squared_error)\n______\n\nnp.random.seed(42)",
      "hint": "You need: numpy, matplotlib.pyplot, sklearn.preprocessing.PolynomialFeatures, sklearn.linear_model.LinearRegression, sklearn.linear_model.Ridge, sklearn.model_selection.train_test_split, sklearn.model_selection.learning_curve, sklearn.metrics.mean_squared_error",
      "solution_line": "import numpy as np\nimport matplotlib.pyplot as plt\nfrom sklearn.preprocessing import PolynomialFeatures\nfrom sklearn.linear_model import LinearRegression, Ridge\nfrom sklearn.model_selection import train_test_split, learning_curve\nfrom sklearn.metrics import mean_squared_error"
    },
    {
      "cell_index": 2,
      "type": "code",
      "source": "# TODO: Define a true function and generate 40 samples with noise\n# f(x) = 0.3*x^3 - x + 2 + N(0, 2)\n\ndef true_function(x):\n    ______  # implement the cubic\n\nn = 40\nX = np.sort(np.random.uniform(-4, 4, n)).reshape(-1, 1)\ny = ______  # apply true_function + gaussian noise(std=2)\n\nX_train, X_val, y_train, y_val = train_test_split(X, y, test_size=0.25, random_state=42)\nprint(f'Train: {len(X_train)} samples | Val: {len(X_val)} samples')",
      "hint": "true_function returns 0.3 * x**3 - x + 2. For y, call true_function(X.ravel()) + np.random.normal(0, 2, n).",
      "solution_line": "def true_function(x):\n    return 0.3 * x**3 - x + 2\n\ny = true_function(X.ravel()) + np.random.normal(0, 2, n)"
    },
    {
      "cell_index": 3,
      "type": "code",
      "source": "# TODO: Plot train/val scatter + true function\nplt.figure(figsize=(8,4))\n# Plot training points (blue circles)\n______\n# Plot validation points (red triangles)\n______\n# Plot true function (green dashed)\nxs = np.linspace(-4, 4, 300)\n______\nplt.xlabel('x'); plt.ylabel('y'); plt.legend()\nplt.title('Synthetic Data: Cubic with Noise')\nplt.show()",
      "hint": "Use plt.scatter(X_train, y_train, label='Train'), plt.scatter(X_val, y_val, marker='^', label='Val'), plt.plot(xs, true_function(xs), 'g--', label='True').",
      "solution_line": "plt.scatter(X_train, y_train, label='Train', alpha=0.7)\nplt.scatter(X_val, y_val, marker='^', label='Validation', alpha=0.7)\nplt.plot(xs, true_function(xs), 'g--', label='True function')"
    },
    {
      "cell_index": 4,
      "type": "code",
      "source": "# TODO: Sweep polynomial degrees 1-12 and record train/val MSE\ndegrees = list(range(1, 13))\ntrain_mse, val_mse = [], []\n\nfor deg in degrees:\n    model = Pipeline([\n        ('poly', PolynomialFeatures(degree=deg)),\n        ('lin', LinearRegression())\n    ])  # NOTE: import Pipeline from sklearn.pipeline\n    ______  # fit on training data\n    ______  # append training MSE\n    ______  # append validation MSE\n\n# TODO: Plot both curves\nplt.figure(figsize=(8,4))\n______  # plot train_mse (blue)\n______  # plot val_mse (red)\nplt.xlabel('Polynomial Degree'); plt.ylabel('MSE')\nplt.legend(); plt.title('Degree Sweep')\nplt.show()",
      "hint": "model.fit(X_train, y_train); train_mse.append(mean_squared_error(y_train, model.predict(X_train))); val_mse.append(mean_squared_error(y_val, model.predict(X_val))). For plotting: plt.plot(degrees, train_mse, 'bo-', label='Train'); plt.plot(degrees, val_mse, 'rs-', label='Validation').",
      "solution_line": "model.fit(X_train, y_train)\ntrain_mse.append(mean_squared_error(y_train, model.predict(X_train)))\nval_mse.append(mean_squared_error(y_val, model.predict(X_val)))\n# Plotting\nplt.plot(degrees, train_mse, 'bo-', label='Train')\nplt.plot(degrees, val_mse, 'rs-', label='Validation')"
    },
    {
      "cell_index": 5,
      "type": "markdown",
      "content": "**Question**: At what degree does validation error start increasing? What does that tell you?\n\n> Write your answer here."
    },
    {
      "cell_index": 6,
      "type": "code",
      "source": "# TODO: Apply Ridge regularisation at the worst-overfitting degree\nworst_deg = ______  # pick the highest degree tested\n\nridge_model = Pipeline([\n    ('poly', PolynomialFeatures(degree=worst_deg)),\n    ('ridge', Ridge(alpha=1.0))\n])\nridge_model.______  # fit\nridge_val_mse = ______  # compute val MSE\n\nunreg_val_mse = val_mse[worst_deg - 1]\nprint(f'Unregularised val MSE (deg {worst_deg}): {unreg_val_mse:.3f}')\nprint(f'Ridge regularised val MSE (deg {worst_deg}): {ridge_val_mse:.3f}')\nprint(f'Improvement: {(1 - ridge_val_mse/unreg_val_mse)*100:.1f}%')",
      "hint": "worst_deg = 12. ridge_model.fit(X_train, y_train). ridge_val_mse = mean_squared_error(y_val, ridge_model.predict(X_val)).",
      "solution_line": "worst_deg = 12\nridge_model.fit(X_train, y_train)\nridge_val_mse = mean_squared_error(y_val, ridge_model.predict(X_val))"
    },
    {
      "cell_index": 7,
      "type": "code",
      "source": "# TODO: Generate learning curves for degree 3 (good) and degree 12 (overfit)\nfig, axes = plt.subplots(1, 2, figsize=(12, 4))\nfor ax, deg in zip(axes, [3, 12]):\n    model = Pipeline([\n        ('poly', PolynomialFeatures(degree=deg)),\n        ('lin', LinearRegression())\n    ])\n    ______, ______, ______ = learning_curve(model, X, y, cv=5,\n         scoring='neg_mean_squared_error',\n         train_sizes=np.linspace(0.1, 1.0, 10))\n    # Convert negative MSE to positive RMSE\n    ax.plot(train_sizes, np.sqrt(-train_scores.mean(axis=1)), 'bo-', label='Train')\n    ax.plot(train_sizes, np.sqrt(-val_scores.mean(axis=1)), 'rs-', label='Val')\n    ax.set_title(f'Degree {deg} Learning Curves')\n    ax.set_xlabel('Samples'); ax.set_ylabel('RMSE'); ax.legend()\nplt.tight_layout(); plt.show()",
      "hint": "train_sizes, train_scores, val_scores = learning_curve(...)",
      "solution_line": "train_sizes, train_scores, val_scores = learning_curve(model, X, y, cv=5, scoring='neg_mean_squared_error', train_sizes=np.linspace(0.1, 1.0, 10))"
    },
    {
      "cell_index": 8,
      "type": "markdown",
      "content": "## Final Checklist\n\n- [ ] I can identify overfitting from a train/val error plot\n- [ ] I can identify underfitting from a train/val error plot\n- [ ] I applied regularisation and observed its effect\n- [ ] I interpreted learning curves correctly\n- [ ] I understand that more data reduces variance but not bias"
    }
  ]
}