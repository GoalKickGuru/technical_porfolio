python3 startup.py

class HashtagsCreator:
    def __init__(self, list_of_terms):
        self.hashtags = []
        
        for term in list_of_terms:
            # Clean the term: remove @ if present
            if term.startswith('@'):
                term = term[1:]   # Remove the @ symbol
            
            # Add # only if it doesn't already have one
            if not term.startswith('#'):
                term = '#' + term
            
            self.hashtags.append(term)
    
    def list_hashtags(self):
        for hashtag in self.hashtags:
            print(hashtag)




class HashtagsCreator:
    def __init__(self, list_of_terms):
        self.hashtags = []

        for term in list_of_terms:
            if term.startswith('@') or term.startswith('#'):
                term = term[1:]
            term = '#' + term
            self.hashtags.append(term)

    def list_hashtags(self):
        for hashtag in self.hashtags:
            print(hashtag)
